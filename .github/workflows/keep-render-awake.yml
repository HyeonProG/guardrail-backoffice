name: Keep Render Awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render health check
        run: |
          curl -fsS --max-time 10 https://guardrail-backoffice.onrender.com/api/v1/health > /dev/null
